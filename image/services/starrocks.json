{
  "name": "${RANGER_STARROCKS_SERVICE_NAME}",
  "type": "starrocks",
  "configs": {
    "username": "${RANGER_STARROCKS_USERNAME}",
    "password": "${RANGER_STARROCKS_PASSWORD}",
    "jdbc.driverClassName": "com.mysql.cj.jdbc.Driver",
    "jdbc.url": "${RANGER_STARROCKS_JDBC_URL}"
  }
}
