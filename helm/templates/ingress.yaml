{{- if and .Values.serve.enabled .Values.serve.ingress.enabled }}
{{- $fullName := include "ranger.fullname" . }}
{{- $svcPort := .Values.serve.service.port }}
{{- $ingress := .Values.serve.ingress }}
{{- $derivedHost := include "ranger.derivedIngressHost" . | trim }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "ranger.labels" . | nindent 4 }}
    app.kubernetes.io/component: serve
  {{- with $ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with $ingress.ingressClassName }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- /* TLS only applies to explicit hosts (derived host has no secret name). */}}
  {{- if $ingress.hosts }}
  {{- $tlsHosts := list }}
  {{- range $ingress.hosts }}
    {{- if and .tls .tls.enabled }}
      {{- $tlsHosts = append $tlsHosts . }}
    {{- end }}
  {{- end }}
  {{- if $tlsHosts }}
  tls:
    {{- range $tlsHosts }}
    - hosts:
        - {{ .name | quote }}
      {{- with .tls.secretName }}
      secretName: {{ . }}
      {{- end }}
    {{- end }}
  {{- end }}
  {{- end }}
  rules:
    {{- if $ingress.hosts }}
    {{- range $ingress.hosts }}
    - host: {{ .name | quote }}
      http:
        paths:
          - path: {{ $ingress.path }}
            pathType: {{ $ingress.pathType }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
    {{- end }}
    {{- else if $derivedHost }}
    - host: {{ $derivedHost | quote }}
      http:
        paths:
          - path: {{ $ingress.path }}
            pathType: {{ $ingress.pathType }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
    {{- end }}
{{- end }}
